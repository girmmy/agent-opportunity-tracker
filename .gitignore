node_modules/
.next/
out/
build/

# Secrets — never commit. Vercel env vars hold the real values.
.env
.env.local
.env*.local

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
next-env.d.ts
*.tsbuildinfo

# Real job-search data. Already loaded into Supabase; the repo ships
# data/seed.example.json instead so this can be public without publishing
# an application history (or anyone else's contact details).
data/seed.json
